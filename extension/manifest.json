{
  "manifest_version": 3,
  "name": "Grok2API in-Space signer",
  "version": "1.0.0",
  "description": "Seeds the grok.com session cookie into this headed browser, then skims fresh x-statsig-id signatures and pushes them to the local proxy. Runs as a normal extension (no CDP), so grok renders the SVG canary and the sigs are valid.",
  "permissions": ["cookies", "tabs", "storage", "webNavigation"],
  "host_permissions": ["https://grok.com/*", "https://*.grok.com/*", "http://127.0.0.1/*", "http://localhost/*"],
  "background": { "service_worker": "background.js" },
  "content_scripts": [
    {
      "matches": ["https://grok.com/*"],
      "js": ["hook.js"],
      "run_at": "document_start",
      "world": "MAIN"
    },
    {
      "matches": ["https://grok.com/*"],
      "js": ["bridge.js"],
      "run_at": "document_start",
      "world": "ISOLATED"
    }
  ]
}
